version: "3.8"

services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    user: root
    restart: always
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins-data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - ../simple-django-app-taller2:/workspace # ← Montar el código
    networks:
      - devopsnet

  app:
    build: ./app
    container_name: django-app
    restart: on-failure
    ports:
      - "8000:8000"
    depends_on:
      - jenkins
    networks:
      - devopsnet

volumes:
  jenkins-data:

networks:
  devopsnet:
    driver: bridge
